[project]
name = "django-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hugo Lowe", email = "lowe.hugo@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "django-stubs>=6.0.9",
]

[project.scripts]
django-test = "django_test:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
